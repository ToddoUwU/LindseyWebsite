<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
      Force all traffic onto HTTPS. Undertow redirects requests that arrive on the
      http-listener to its https socket (http-listener redirect-socket="https").
      There is intentionally NO <auth-constraint> here, so this enforces transport
      security only; authentication/authorization is handled by Spring Security.
    -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Entire application</web-resource-name>
            <url-pattern>/*</url-pattern>
        </web-resource-collection>
        <user-data-constraint>
            <transport-guarantee>CONFIDENTIAL</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

</web-app>
